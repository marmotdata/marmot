name: "openlineage_test"
version: "1.0.0"
config-version: 2

profile: "openlineage_test"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  openlineage_test:
    +materialized: table
