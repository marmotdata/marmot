version: 2
project_name: marmot-plugin-hive

# gohive is pure Go over Thrift unless built with the kerberos tag, which
# needs the GSSAPI C library. Releases skip that tag, so this plugin builds
# like the other pure-Go plugins: one static binary per platform.
env:
  - CGO_ENABLED=0

builds:
  - main: .
    binary: marmot-plugin-hive
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    flags:
      - -trimpath
    ldflags:
      - -s -w

archives:
  - id: marmot-plugin-hive
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md

checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_checksums.txt"
  algorithm: sha256

signs:
  - cmd: cosign
    signature: "${artifact}.sig"
    certificate: "${artifact}.pem"
    args:
      - sign-blob
      - --yes
      - --output-signature=${signature}
      - --output-certificate=${certificate}
      - ${artifact}
    artifacts: checksum
    output: true

changelog:
  disable: true

release:
  disable: true
